#include <iostream>
using namespace std;
int main() {
	int first, second, sum;
	cout << "Введите два целых числа: ";
	cin >> first >> second;
	sum = first + second;
	cout << first << " + " << second << " = " << sum << endl;
	return 0;
}